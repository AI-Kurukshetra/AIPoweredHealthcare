# Supabase Workspace

This folder is reserved for Supabase project-local artifacts that are app-specific
but not part of migration SQL (for example, generated policies snapshots, seed
helpers, or local scripts).
